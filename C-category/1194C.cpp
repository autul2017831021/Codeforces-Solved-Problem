#include<bits/stdc++.h>
#include<string.h>
using namespace std;
main()
{
    int n;
    cin>>n;
    while(n--){
    char s[101],p[101],t[101];
    scanf("%s %s %s",s,t,p);
    int ls=strlen(s);
    int lt=strlen(t);
    int lp=strlen(p);


    }
}
